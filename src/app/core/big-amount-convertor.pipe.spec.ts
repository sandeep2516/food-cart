import { BigAmountConvertorPipe } from './big-amount-convertor.pipe';

describe('BigAmountConvertorPipe', () => {
  it('create an instance', () => {
    const pipe = new BigAmountConvertorPipe();
    expect(pipe).toBeTruthy();
  });
});
