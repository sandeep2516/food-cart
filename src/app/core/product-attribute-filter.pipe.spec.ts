import { ProductAttributeFilterPipe } from './product-attribute-filter.pipe';

describe('ProductAttributeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductAttributeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
